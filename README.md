# 💝 Valentine Bot - Telegram бот для отправки валентинок

Полнофункциональный Telegram-бот для отправки анонимных и подписанных валентинок к 14 февраля!

## ✨ Основные функции

✅ **Отправка валентинок** - текстовые и с картинками  
✅ **Шаблоны картинок** - автоматическое форматирование текста  
✅ **Выбор адресата** - через @username  
✅ **Анонимность** - опция скрытия отправителя  
✅ **Очередь** - валентинки невидимым пользователям отправляются при первом входе  
✅ **Inline режим** - рандомные комплименты по команде  
✅ **Админ панель** - статистика и рассылка сообщений  

## 📋 Требования

- Python 3.8+
- pip (менеджер пакетов)

## 🚀 Установка и запуск

### 1. Загрузите архив и распакуйте его

```bash
unzip valentine-bot.zip
cd valentine-bot
```

### 2. Создайте виртуальное окружение (рекомендуется)

```bash
python -m venv venv

# Windows:
venv\Scripts\activate

# Linux/Mac:
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Получите токен бота

- Откройте Telegram и найдите бота **@BotFather**
- Отправьте команду `/newbot`
- Следуйте инструкциям и получите токен

### 5. Создайте файл .env

Скопируйте `.env.example` в `.env` и добавьте ваши данные:

```bash
cp .env.example .env
```

Откройте `.env` и заполните:
```
BOT_TOKEN=your_bot_token_here
ADMIN_ID=your_telegram_id_here
```

**Как узнать свой Telegram ID?**
- Напишите боту **@userinfobot**
- Он покажет ваш ID

### 6. Добавьте шаблоны и шрифт (опционально)

Если хотите использовать валентинки с картинками:

1. Создайте папку `templates`
2. Добавьте 3 шаблона изображений:
   - `template1.png` (размер ~800x600)
   - `template2.png` (размер ~800x600)
   - `template3.png` (размер ~800x600)

3. Создайте папку `fonts`
4. Добавьте шрифт `Involve.ttf`

**Где взять шрифт Involve?**
- Скачайте с Google Fonts: https://fonts.google.com/specimen/Involve
- Или используйте любой другой TTF шрифт, изменив путь в `config.py`

### 7. Запустите бота

```bash
python main.py
```

Бот будет запущен и ждать команд!

## 🎮 Как пользоваться

### Для обычных пользователей:

1. **Напишите /start** - откроется главное меню
2. **Отправить валентинку** - выберите тип (текст или картинку)
3. **Выберите адресата** - введите @username
4. **Напишите текст** - максимум 250 символов
5. **Выберите шаблон** (если картинка) - 3 варианта
6. **Выберите режим** - анонимно или с подписью
7. **Готово!** - валентинка отправлена

### Для админа:

1. **Админ панель** - просмотрите статистику
2. **Рассылка** - отправьте сообщение всем пользователям

### Inline режим:

Любому пользователю введите в чат: `@your_bot_name комплимент`
Бот отправит рандомный комплимент!

## 📁 Структура проекта

```
valentine-bot/
├── config.py          # Конфигурация и константы
├── database.py        # Работа с SQLite БД
├── handlers.py        # Обработчики команд и сообщений
├── utils.py           # Утилиты для обработки изображений
├── admin_panel.py     # Панель администратора
├── main.py            # Главный файл бота
├── requirements.txt   # Зависимости
├── .env.example       # Пример переменных окружения
├── .env               # Переменные окружения (создайте вручную)
├── valentine_bot.db   # База данных (создается автоматически)
├── templates/         # Шаблоны для картинок
├── fonts/             # Шрифты для текста
└── README.md          # Этот файл
```

## 🛠️ Конфигурация

Все параметры находятся в файле `config.py`:

```python
BOT_TOKEN = "ваш_токен"        # Токен от @BotFather
ADMIN_ID = 123456789            # Ваш Telegram ID
MAX_TEXT_LENGTH = 250           # Максимальная длина текста
TEMPLATES_PATH = "templates"    # Папка с шаблонами
FONTS_PATH = "fonts/Involve.ttf" # Путь к шрифту
```

## 📊 База данных

Бот использует SQLite для хранения:
- **users** - информация о пользователях
- **valentines** - отправленные валентинки
- **queue** - валентинки в очереди для невидимых пользователей

База данных создается автоматически при первом запуске!

## 🐛 Решение проб��ем

### Бот не запускается

Проверьте:
- ✅ Установлены ли все зависимости: `pip install -r requirements.txt`
- ✅ Заполнен ли файл `.env` с правильным токеном
- ✅ Python версия 3.8 или выше

### Валентинки с картинками не работают

Проверьте:
- ✅ Существует ли папка `templates` с файлами `.png`
- ✅ Существует ли папка `fonts` с файлом `.ttf`
- ✅ Пути указаны правильно в `config.py`

### Админ панель не открывается

Проверьте:
- ✅ Ваш ID совпадает с `ADMIN_ID` в файле `.env`
- ✅ Используйте команду `/start` в боте, чтобы обновить меню

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли
2. Убедитесь, что все файлы на месте
3. Перезагрузите бота: нажмите Ctrl+C и снова `python main.py`

## 📄 Лицензия

MIT License - свободно используйте в своих целях!

## 💝 Удачи!

Надеюсь, ваши друзья получат много красивых валентинок! 🎉

---

**Создано с ❤️ для влюбленных**